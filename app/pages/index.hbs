<div class="hero min-h-screen bg-base-200">
  <div class="hero-content flex-col lg:flex-row sm:gap-8 ">
    <div class="flex flex-col gap-4 items-center lg:items-start text-center lg:text-left">
      <h1 class="text-5xl font-bold">What's on your mind?</h1>
      <p class="text-xl">
        {{globalData.name}} is a platform where you can share your thoughts with the world. 
        Whether you're a writer, photographer, sports fanatic or tech enthusiast, {{globalData.name}} is the perfect place to speak your mind.
      </p>
      {{#if currentUser}}
        <a class="btn btn-primary" href="/{{currentUser.details.username}}">View Blogs</a>
      {{else}}
        <a class="btn btn-primary" href="/user/signup">Get Started</a>
      {{/if}}
    </div>
    {{> carousel-blog blogs=globalData.dummyBlogs range=globalData.dummyBlogs.length}}
  </div>
</div>